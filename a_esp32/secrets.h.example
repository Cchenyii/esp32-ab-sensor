// Copy this file to secrets.h and fill in your WiFi credentials.
// secrets.h is gitignored and must not be committed.

#pragma once

static const char* WIFI_SSID = "YOUR_WIFI_SSID";
static const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// If mDNS fails on phone hotspot, put Board B OLED IP here (e.g. "192.168.43.12").
// Leave empty "" to use mDNS + cache only.
static const char* SERVER_IP_FALLBACK = "";
